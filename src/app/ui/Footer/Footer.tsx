import React from 'react';

export type FooterProps = {}

const Footer = (props: FooterProps) => {
  return (
    <div className={'p-4'}>
      // TODO: Footer
    </div>
  );
};

export default Footer;